apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: image-builder

resources:
  - namespace.yaml
  - rbac.yaml
  - pvc.yaml
  - configmap.yaml
  - workflow.yaml
  - cronworkflow.yaml
